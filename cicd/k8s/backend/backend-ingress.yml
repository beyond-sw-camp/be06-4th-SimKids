apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: backend-ingress-old
spec:
  ingressClassName: nginx
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: backend-svc-old
                port:
                  number: 8080